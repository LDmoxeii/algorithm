package com.only4.algorithm.version4j.leetcode;

import org.junit.jupiter.api.Test;

class TrapTest {
    private Trap target = new Trap();

    @Test
    void TC001() {
        target.trap(new int[]{4, 2, 0, 3, 2, 5});
    }
}
